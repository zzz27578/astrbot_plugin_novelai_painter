# NovelAI Painter 2.2.0 更新与功能保证报告

## 修复目标

本版本针对实际日志暴露的三条故障链进行修复：预设人物/画风没有进入最终提示词、工具完成后继续循环调用、循环耗尽后 Agent 转而调用无关沙箱工具。同时处理仓库审查发现的固定命令、权限、参考图、禁用状态、重试和响应安全问题。

## 已完成修复

### 1. 人设与预设绑定

- `persona_preset_map` 是运行时权威映射。
- WebUI 预设编辑器保存 `persona_id` 时，会立即同步该映射；改绑和解绑会清理旧映射。
- 独立人设映射表是权威数据；预设编辑器不会用陈旧的内嵌字段覆盖用户后来选择的映射，页面回显也以权威映射为准。
- 2.1.x 已保存但从未生效的 `preset.persona_id` 会在插件启动时迁移，已有显式映射不会被覆盖。
- 运行时通过 AstrBot 4.27.3+ 的 `PersonaManager.resolve_selected_persona()` 解析会话强制人设、Conversation 人设和默认人设，再匹配插件映射；结果与主 Agent 的人设选择路径保持一致。
- 映射解析顺序为：显式 preset ID、AstrBot 当前生效人设映射、旧版内嵌 persona ID、默认预设。
- 禁用预设在所有自动解析路径中都会被忽略。

### 2. 最终提示词与冲突保护

- 后端请求只接收 `_compose_prompt()` 的合成结果，不直接使用 LLM 原始提示词。
- NovelAI V4/V5 继续使用数值权重，人物和画风锚定词位于动作/场景要求之前。
- 当人物或画风锁定，且用户没有明确要求修改对应内容时，会删除 LLM 自行补写的头发、眼睛、耳朵、服装、配饰和画风标签。
- 用户明确提出换装、修改发色或更换画风时，对应标签仍会保留。

### 3. 参考图

- 新增 `llm_auto_reference`，默认启用。
- 当前生效预设绑定参考图时，NovelAI 官方后端的 LLM 普通生图自动升级为 Precise Reference。
- OpenAI 兼容后端自动使用图片编辑/图生图端点。
- LLM 工具也可明确传入 `generate`、`img2img` 或 `reference`，以及 `character`、`style`、`both` 参考类型。
- LLM 未显式传参考类型时继承预设保存的 `character`、`style` 或 `both`，不再被工具参数默认值覆盖。
- 命令级参考类型作为单次参数传递，不再修改共享预设对象。

### 4. Agent 循环与额度保护

- 图片或最终错误已直接发送给用户后，LLM 工具返回 `None`，符合 AstrBot 4.27.3+ 的直接结果终止协议。
- 相同入站消息仍由事件 claim、事件指纹、进行中任务表和 Job ID 发送表多层去重。
- 即使模型在同一轮产生多个生图调用，最多只有一次后端任务和一次图片发送。
- 只有明确的 HTTP 429 可重试，并同时受重试策略与 `max_api_requests_per_job` 硬上限约束。
- Retry-After 等待发生在全局请求锁之外，不会无意义地阻塞其他用户。

### 5. WebUI、权限和安全

- 预设编辑器增加启用开关、人设 ID 候选列表、绑定生效说明和禁用状态标识。
- `admin_only` 始终允许管理员；`admin_bypass` 只绕过白名单，不能绕过 disabled。
- 创建和更新统一执行名称、长度、参考类型、参考 ID、强度和布尔值校验，写入失败会回滚内存状态。
- API 响应限制为 48MB，最终图片和参考下载限制为 32MB；ZIP 解压前检查未压缩大小。
- 任务记录不再向 WebUI API 暴露服务器本地图片路径。

## 兼容基线与自动验证

- 最低支持版本为 AstrBot 4.27.3；已对照 4.27.3 与 4.27.5 源码核验人设解析签名、Pages Web API 注册签名和本地 LLM 工具直接结果终止语义。
- 33 项 Python 单元测试覆盖 AstrBot 4.27.3+ PersonaManager 调用参数、提示词合成、旧绑定迁移、WebUI 权威映射同步、禁用预设、冲突标签过滤、参考图类型继承与自动选择、Agent 终止返回、重复任务、固定命令完整提示词、权限、429 重试硬上限、单图发送和 WebUI 关键控件。
- JavaScript 通过 `node --check`。
- 配置 Schema 通过 JSON 解析。
- `git diff --check` 无空白错误。

## 功能保证边界

本版本可以保证：命中启用预设时，人物和画风锚定词会进入实际 API payload；WebUI 人设绑定会进入运行时映射；绑定参考图会按配置进入对应后端请求；一次入站消息只建立一个生图任务且图片只发送一次；只有管理员明确启用的 429 重试可以增加 API 尝试次数，并受 1–3 次硬上限约束；工具直接发送最终结果后不会继续 Agent 循环。

生成模型具有随机性，因此不能保证每张图在视觉上 100% 复现人物。代码层面通过锚定权重、参考图和冲突标签过滤降低漂移，但最终一致性仍受模型、采样参数、参考图质量和服务端实现影响。
