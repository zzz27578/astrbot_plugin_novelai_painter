# NovelAI Painter 2.4.0 更新与功能保证报告

## 更新目标

本版本新增独立的表情包模式：普通 LLM 回复完成后，插件根据配置概率、会话上下文、最终回复和情绪标签，判断是否为当前语境生成一张表情包，并在文字回复之后或同一消息链中发送。

## WebUI 配置

“表情包模式”是设置页的独立页签，包含：

- 总开关和触发概率。
- 文字回复后发送或与文字同一气泡。
- 会话冷却时间和判断上下文条数。
- 判断模型 ID 和判断超时。
- 表情包图片宽高。
- 是否允许主 LLM 提供情绪标签。
- 是否自动使用表情包角色卡绑定的参考图。
- 通用提示词、判断规则，以及独立的表情包角色卡。

表情包角色卡拥有独立的正向 Tag、负面 Tag、锚定强度、参考图和参考类型，不会修改普通角色卡。保存配置时服务端会校验数值范围、枚举、参考图 ID 和角色卡字段长度。

## 运行流程

1. 表情包模式关闭时，不执行本次表情包处理路径，不影响普通聊天和普通生图。
2. 模式开启后，插件在普通 LLM 请求中保存最近上下文，并向主 LLM 说明可调用 `novelai_set_emotion` 提供内部情绪元数据。
3. 主 LLM 返回最终文字后，插件先执行概率和冷却检查，再调用判断模型。
4. 判断模型只需要返回 JSON：`send`、`emotion`、`prompt` 和 `reason`。其中 `prompt` 是英文视觉描述，只负责情绪、动作、构图和场景，不重写表情包角色卡。
5. 判断通过后，插件使用现有 `_run_job` 生图路径，复用后端选择、角色卡 Tag 合成、负面 Tag、参考图、请求重试、单任务去重、任务记录和临时文件清理。
6. 生成失败、判断失败、超时、概率未命中或处于冷却期时，原文字回复不受影响。

## 发送行为

“文字回复后发送”会在文字消息完成后额外发送一条图片消息。“与文字同一气泡”会在结果装饰阶段把文字和图片合并成同一条 `MessageChain`，并自动关闭本次回复的流式输出，保证图片不会晚于文字单独发送。

表情包任务使用独立的 `kind=sticker` 任务记录和配置尺寸。已有图片回复会跳过表情包处理，避免一条消息重复发送多张图片。每条表情包任务仍最多读取和发送一张图片。

## 兼容与安全边界

- 配置版本由 5 升至 6；旧配置缺少表情包字段时自动补齐，默认关闭。
- 需要 AstrBot `>=4.27.3`，依赖现有插件 Pages Web API、LLM request/response 事件和消息链接口。
- 判断模型的 JSON 输出和生图模型的视觉服从程度由第三方模型决定；插件对非 JSON、超时和异常统一降级为不发送表情包。
- 冷却以 `unified_msg_origin` 为会话键，仅保存在当前插件进程内，重启后清空。
- 表情包参考图继续使用插件数据目录中的安全 ID 和现有参考图校验，不接受路径输入。
- 表情包生成失败不会回滚或重复发送已经完成的文字回复。

## 验证结果

- Python 单元测试：45 项通过。
- Python 语法检查：`python -m py_compile main.py tests/test_core.py` 通过。
- JavaScript 语法检查：`node --check pages/settings/app.js` 通过。
- 配置 Schema：`python -m json.tool _conf_schema.json` 通过。
- Git 空白检查：`git diff --check` 通过。
- 桌面浏览器视觉检查：当前环境的 Computer Use 内核初始化失败，错误为 `failed to write kernel assets: 系统找不到指定的路径。`，因此未将视觉截图结果作为通过证据；已完成 HTML/CSS/JavaScript 静态复核。

新增测试覆盖表情包 JSON 解析、同一气泡文字与图片合并、情绪工具元数据隔离和最终 LLM 回复捕获。
