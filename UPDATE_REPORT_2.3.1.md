# NovelAI Painter 2.3.1 更新与功能保证报告

## 修复目标

本版本处理四类实际使用问题：LLM 在首个生图调用尚未完成收尾时继续发起相同工具调用、角色卡保护开关容易被理解为禁止主动换装、任务记录无法清理，以及固定命令缺少角色卡与 NovelAI 模型管理能力。

## 工具入口级并发保护

`novelai_generate_image` 现在具有独立的入口闸门。第一次调用会在以下操作之前同步抢占当前入站消息：

- 解析 AstrBot 当前人设和角色卡映射。
- 过滤或拼接提示词。
- 读取角色卡参考图。
- 进入任务队列或取得全局请求锁。
- 调用 NovelAI 或兼容后端。

同一入站消息的后续工具调用会立即返回 `ENTRY_GATE_BLOCKED_DUPLICATE`，不会执行上述准备工作，更不会进入官网请求路径。事件对象标记与消息 ID 映射共同覆盖顺序重复和并发重复；映射至少保留 300 秒，避免模型收尾较慢时超过普通短窗口。

原有 `_run_job` 事件 claim、消息指纹缓存、inflight 任务表、单任务请求上限和 Job ID 图片发送表全部保留。即使入口闸门因异常平台事件对象失效，后续层仍会在官网请求前拦截。

插件无法阻止第三方 LLM 产生“调用工具”这个 token，也无法让 AstrBot 不显示一次被拦截的工具记录；可以保证的是，被入口闸门拦截的调用不会解析参考图、排队或访问 NovelAI 官网，因此不会形成官网并发或重复扣费。

## 角色卡一致性保护

WebUI 原“锁定角色卡主体与画风”更名为“保护未指定的角色与画风特征”。该开关的语义是：

- 用户没有提出修改时，过滤 LLM 擅自补写的人物、发型、发色、眼睛、种族、服装和画风 Tag。
- 用户明确要求换衣、改发型、改主体或改画风时，对应类别正向与负向 Tag 仅在本次请求让位。
- 角色卡其他类别继续保护，保存到磁盘的角色卡不会被改写。

除直接识别婚纱、裙装、头发、画风等关键词外，本版本会识别“换、改、变成、替换、change、replace、switch”等修改意图。当用户使用“换个造型”等没有直接说出具体类别的表达时，再依据 LLM 翻译出的本次 Tag 确定需要临时放行的类别。已经从用户原文识别出类别时，不会信任 LLM 额外补写的其他类别，避免“只要求换衣服却顺便变成粉毛”。

角色卡运行时绑定现在只以 `persona_preset_map` 为准。旧 `preset.persona_id` 仍会在启动时迁移到权威映射，但不再作为运行时回退，确保 WebUI 或命令清除映射后不会被旧字段重新绑定。

## 固定命令

仅发送 `/nai` 或 `/nai help` 会显示完整帮助。命令前缀仍可在 WebUI 修改，以下示例使用默认前缀：

```text
/nai draw <画面描述>
/nai img2img <画面描述>
/nai reference <character|style|both> <画面描述>
/nai card current
/nai card list
/nai card use <角色卡 ID 或完整名称>
/nai card clear
/nai model current
/nai model list
/nai model use <模型 ID 或别名>
```

`card use` 优先修改当前 AstrBot 人设的持久映射；没有当前人设时修改默认角色卡。`card clear` 按相同作用域解除。禁用角色卡不能通过命令启用或切换，避免绕过 WebUI 状态。

`model use` 仅在 NovelAI 官方后端生效，目标必须是 WebUI 已列出的固定模型。支持完整模型 ID，也支持 `v5`、`v5-curated`、`v4.5`、`v4.5-curated`、`v4`、`v4-curated`、`v3`、`v3-furry` 等别名。角色卡和模型写入失败会恢复修改前配置。

所有帮助、查看、切换与生图命令统一受以下 WebUI 配置控制：

- 入口模式必须包含“固定命令”。
- 私聊与群聊权限。
- 管理员策略及用户/群聊白名单。

同时修复了命令处理器在确认前缀之前检查入口模式的问题；其他插件的斜杠命令不会再被 NovelAI Painter 错误回复“当前未启用”。

## WebUI 任务记录

- 每条任务记录新增“删除记录”按钮。
- 页面顶部新增“清空记录”按钮，无记录时自动禁用。
- 单条删除和全部清空都使用页面内确认对话框。
- 请求期间按钮进入禁用/忙碌状态，完成后显示成功或错误反馈。
- 点击区域至少 44px，并保留键盘焦点、Esc 取消、移动端单列回流和 reduced-motion 支持。
- 删除只清理本次插件进程的内存任务记录，不会撤回消息或删除已经发送的图片。

## 验证结果

- Python 单元测试：41 项通过。
- AstrBot 4.27.3 源码环境：41 项通过。
- AstrBot 4.27.5 源码环境：41 项通过。
- JavaScript：`node --check pages/settings/app.js` 通过。
- 配置 Schema：`python -m json.tool _conf_schema.json` 通过。
- Python 语法：`python -m py_compile main.py tests/test_core.py` 通过。
- Git 空白检查：`git diff --check` 通过。

新增回归覆盖包括：第二次 LLM 工具调用在角色卡解析和任务创建前被拒绝、官网 provider 只调用一次、角色卡列表/查看/切换/解除、NAI 模型列表与切换、只发送 `/nai` 的完整帮助、命令权限拒绝、其他斜杠命令不被误拦截、权威人设映射、单条任务删除、任务全部清空和 WebUI 删除控件。

## 功能保证边界

2.3.1 可以保证同一条入站消息的重复 LLM 生图调用在插件入口被拦截，拦截调用不会访问 NovelAI 官网；可以保证角色卡和模型管理命令由固定命令权限统一控制；可以保证明确的换装、发型和画风修改不会因为一致性保护开关而被无条件屏蔽；可以保证 WebUI 可删除内存任务记录。

第三方 LLM 是否仍尝试产生重复工具调用，以及生成模型对 Tag 的视觉服从程度，不由插件代码完全控制。入口闸门负责把重复调用变成无后端副作用的即时结果；角色卡正负 Tag、参考图和分类覆盖负责降低人物与画风漂移，但不承诺像素级一致。
